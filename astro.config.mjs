import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // site: 'https://gnnl.tv',
  base: '/GNNL_TV', // your repo name
  // Configure your site settings here
});
